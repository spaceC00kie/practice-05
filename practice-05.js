//given an array of integers return an array of integers where each element is increased by 2
//for ex if [1, 2, 3] then output should be [3, 4, 5]

const addTwo = (arr) => arr.map((num) => num + 2)

console.log(addTwo([2, 3, 4]))
